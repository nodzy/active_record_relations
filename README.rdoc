App:

- Uses validations for models (using built-in Active Record validations as well as custom validations)
- Uses a grandparent relationship with a :through option
- Uses default_scope queries
- Uses aggregation queries
- Uses SQL snippets and defends itself against SQL injection

